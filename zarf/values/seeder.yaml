# values/seeder.yaml
image:
  repository: rancher/harvester-seeder
  # tag: ###ZARF_CONST_HARVESTER_VERSION###
cluster:
  # These values are templated by Zarf at deploy time
  imageURL: "http://###ZARF_VAR_K3S_HOST_IP###:32200"
  version: "###ZARF_CONST_HARVESTER_VERSION###"

stack:
  publicIP: "###ZARF_VAR_K3S_HOST_IP###"
#   hook:
#     enabled: true
#     name: hook-files
#     port: 8080
#     image: bash
#     downloadsDest: /opt/hook
#     # downloadURL only works with the > 0.8.1 Hook release because
#     # previous Hook versions didn't provide a checksum file.
#     downloadURL: "http://###ZARF_VAR_K3S_HOST_IP###:32200/hook"
# # -- Overrides